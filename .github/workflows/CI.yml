name: CI

on:
  push:
    branches:
      - main
      - develop
      - preparation-mise-en-production
  pull_request:
    branches:
      - main
      - develop
      - preparation-mise-en-production

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4