name: CI

on:
  push:
    branches:
      - main
      - develop
      - preparation-mise-en-production
  pull_request:
    branches:
      - main
      - develop
      - preparation-mise-en-production

jobs:
  build:
    runs-on: ubuntu-latest
