version: "3.9"
services:
  next-app:
    build: .
    ports:
      - "3000:3000"
    networks:
      - dokploy-network
    environment:
      DATABASE_URL: ${DATABASE_URL}


networks:
  dokploy-network:
    external: true
